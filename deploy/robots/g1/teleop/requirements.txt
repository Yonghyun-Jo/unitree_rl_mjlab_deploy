# vr_teleop_bridge.py runtime deps — LAPTOP side only (the G1 onboard does NOT need these;
# the robot runs only the C++ g1_ctrl, and GMR must stay OFF the control host).
#
# GMR (general_motion_retargeting) itself is installed by setup_teleop.sh (editable clone +
# g1 mesh repair), which also pulls GMR's own stack (mujoco, mink, numpy, scipy, ...).
# These are the bridge's direct imports; pyzmq is the one GMR does NOT provide.
numpy
scipy
pyzmq
