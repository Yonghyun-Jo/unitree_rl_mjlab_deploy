# mode5 나머지 키 자세 2개: 진입(직립) → 낮은 포복 → 직립 → 크랩(배위) → 직립 → mode1
# 한 줄 = <밴드 해제 뒤 초> <키>. sim2sim_headless.py --script 로 돌린다(--stand 82).
# 두 자세는 가는 중 z_mask=0(높이 명령 없이 자세로 간다) — 학습 쪽 버튼 검증에 없던 둘이다.
2 5
12 c
28 z
44 h
60 z
76 1
